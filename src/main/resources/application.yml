spring:
  application:
    name: gamarraloop-platform

  datasource:
    url: ${DATABASE_URL:jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres}
    username: ${DATABASE_USER:postgres.xureptopakmgylfrifvf}
    password: ${DATABASE_PASSWORD:haldymiamigO7!}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: ${PORT:8080}
  servlet:
    context-path: /api/v1

# Google Cloud Vision API
google:
  cloud:
    vision:
      api-key: ${VISION_API_KEY:your-api-key-here}
      endpoint: https://vision.googleapis.com/v1/images:annotate
